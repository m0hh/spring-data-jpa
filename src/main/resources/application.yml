spring:
    datasource:
        driver-class-name: org.postgresql.Driver
    config:
        import: db.yml
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
        show-sql: true
